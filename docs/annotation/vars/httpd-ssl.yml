---

task_httpd-ssl_synopsis: |
  
  Configure SSL in extra/httpd-ssl.conf

task_httpd-ssl_description: |
  
  Iterate ``apache_httpd_conf_ssl_extra`` (11) and configure lines in
  ``extra/httpd-ssl.conf``. Iterate ``apache_httpd_conf_ssl_extra_absent`` (22) and remove lines
  from ``extra/httpd-ssl.conf`` (19). Iterate ``apache_httpd_conf_ssl_listen`` (30) and add configuration lines
  in ``extra/httpd-ssl.conf`` (27). Iterate ``apache_httpd_conf_ssl`` (38) and configure lines in
  ``httpd.conf`` (35).

task_httpd-ssl_lines: 3,7,16,25,33

task_httpd-ssl_seealso:
  handlers: []
  templates: []
  others:

    - Variable :ref:`ug_apache_httpd_conf_ssl`
    - Variable :ref:`ug_apache_httpd_conf_ssl_extra`
    - Variable :ref:`ug_apache_httpd_conf_ssl_extra_absent`
    - Variable :ref:`ug_apache_httpd_conf_ssl_listen`

task_httpd-ssl_note: []
task_httpd-ssl_hint: []
task_httpd-ssl_warning: []

# EOF
...
